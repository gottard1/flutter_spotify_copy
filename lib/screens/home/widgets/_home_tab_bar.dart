import 'package:flutter/material.dart';

class _HomeTabBar extends StatelessWidget {
  const _HomeTabBar({super.key});

  @override
  Widget build(BuildContext context) {
    return TabBarView(
      children: [
        Container(),
      ],
    );
  }
}
